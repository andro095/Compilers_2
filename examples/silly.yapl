class Silly {
    copy() : SELF_TYPE { self };
};

class Sally inherits Silly { };

class Main {
    x : Sally <- (new Sally).copy();
    main() : Sally { x };
    otromain(var1: Int, var2: Int): Sally {
     let aaa: Int, bbb: Char in (3 + 3)

    };
};