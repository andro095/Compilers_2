class Main {
    x: String;
    io_enter: IO;

    main() : Int {
        {
            x <- io_enter.in_string();
            x <- x.concat("Hola").concat("Hola2").concat(new String);
            x.length();
        }
    };
};