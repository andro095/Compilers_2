class Main {
    x: Int;
    x2: Int;
    x3: Int;
    x4: Int;
    x5: Int;
    main(): Int {
        {
            x <- 1 + 2;
            x2 <- 1 * 2 / 3;
            x3 <- x - x2;
        }

    };
};